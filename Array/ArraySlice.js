/* Array Slice - this helps to create an array from another Existing array
* Syntax: array.slice(start, end) ; where end  index is exclusive
*/
let arr = [1,2,3,4,5,6,7];
let arr1 = arr.slice(2,4);
console.log(arr1);
